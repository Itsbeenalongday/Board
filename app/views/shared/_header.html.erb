<nav class="navbar navbar-expand-lg navbar-light bg-warning">
  <%= fa_icon "comment", style: "font-size:20px;" %>
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Board</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-between" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <a class="nav-link" href="<%= posts_path %>">전체 게시글<span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item active">
          <a class="nav-link" href="<%= new_post_path %>">게시글 작성<span class="sr-only">(current)</span></a>
        </li>
      </ul>
      <ul class="navbar-nav">
        <% if current_user %>
          <li class="nav-item active">
            <a class="nav-link" href="#"> <%= current_user.name %>님 환영합니다!<span class="sr-only">(current)</span></a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              나의 페이지
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              <a class="dropdown-item" href="<%= edit_user_registration_path %>">회원정보 수정</a>
              <a class="dropdown-item" href="<%= posts_path(type: :owns) %>">내 게시글 보기</a>
              <a class="dropdown-item" href="<%= posts_path(type: :likes) %>">좋아요한 게시글 보기</a>
            </div>
          </li>
           <li class="nav-item active">
            <%= link_to "로그아웃", destroy_user_session_path, method: :delete, class: "nav-link" %>
          </li>
        <% else%>
          <li class="nav-item active">
            <a class="nav-link" href="<%= new_user_session_path %>">로그인<span class="sr-only">(current)</span></a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="<%= new_user_registration_path %>">회원가입<span class="sr-only">(current)</span></a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>